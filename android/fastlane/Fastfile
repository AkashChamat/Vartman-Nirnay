# Disable analytics prompts
opt_out_usage

default_platform(:android)

platform :android do
  ###########################################################
  # ✅ Pre-flight checks before any lane runs
  ###########################################################
  before_all do
    UI.message("🚀 Starting Fastlane deployment process...")

    # Define AAB path (build output)
    aab_path = ENV['AAB_PATH'] || File.expand_path("../app/build/outputs/bundle/release/app-release.aab", __dir__)
    unless File.exist?(aab_path)
      UI.user_error!("❌ AAB file not found at #{aab_path}. Please build the project first.")
    end
    ENV['AAB_PATH'] = aab_path

    # Play Store JSON key file
    json_key_path = ENV['JSON_KEY_PATH'] || File.expand_path("play-store-key.json", __dir__)
    unless File.exist?(json_key_path)
      UI.user_error!("❌ Play Store JSON key not found at #{json_key_path}. Please ensure PLAY_STORE_JSON_KEY is set correctly.")
    end
    ENV['JSON_KEY_PATH'] = json_key_path

    UI.success("✅ Pre-flight checks passed successfully.")
  end

  ###########################################################
  # 🚀 Deployment Lanes
  ###########################################################

  desc "Deploy to Internal Testing"
  lane :internal do
    upload_to_play_store(
      track: 'internal',
      json_key: ENV['JSON_KEY_PATH'],
      aab: ENV['AAB_PATH'],
      package_name: ENV['PACKAGE_NAME'],
      skip_upload_apk: true,
      skip_upload_metadata: true,
      skip_upload_images: true,
      skip_upload_screenshots: true,
      release_status: 'completed'
    )
    UI.success("🎉 Successfully deployed to Internal Testing!")
  end

  desc "Deploy to Alpha Testing"
  lane :alpha do
    upload_to_play_store(
      track: 'alpha',
      json_key: ENV['JSON_KEY_PATH'],
      aab: ENV['AAB_PATH'],
      package_name: ENV['PACKAGE_NAME'],
      skip_upload_apk: true,
      skip_upload_metadata: true,
      skip_upload_images: true,
      skip_upload_screenshots: true,
      release_status: 'completed'
    )
    UI.success("🎉 Successfully deployed to Alpha Testing!")
  end

  desc "Deploy to Beta Testing"
  lane :beta do
    upload_to_play_store(
      track: 'beta',
      json_key: ENV['JSON_KEY_PATH'],
      aab: ENV['AAB_PATH'],
      package_name: ENV['PACKAGE_NAME'],
      skip_upload_apk: true,
      skip_upload_metadata: true,
      skip_upload_images: true,
      skip_upload_screenshots: true,
      release_status: 'completed'
    )
    UI.success("🎉 Successfully deployed to Beta Testing!")
  end

  desc "Deploy to Production"
  lane :production do
    upload_to_play_store(
      track: 'production',
      json_key: ENV['JSON_KEY_PATH'],
      aab: ENV['AAB_PATH'],
      package_name: ENV['PACKAGE_NAME'],
      skip_upload_apk: true,
      skip_upload_metadata: true,
      skip_upload_images: true,
      skip_upload_screenshots: true,
      release_status: 'completed'
    )
    UI.success("🎉 Successfully deployed to Production!")
  end

  ###########################################################
  # 🔁 Promotion Lanes
  ###########################################################

  desc "Promote Internal → Alpha"
  lane :promote_internal_to_alpha do
    upload_to_play_store(
      track: 'internal',
      track_promote_to: 'alpha',
      json_key: ENV['JSON_KEY_PATH'],
      package_name: ENV['PACKAGE_NAME'],
      skip_upload_apk: true,
      skip_upload_aab: true,
      skip_upload_metadata: true,
      skip_upload_images: true,
      skip_upload_screenshots: true
    )
    UI.success("🎉 Promoted Internal → Alpha!")
  end

  desc "Promote Alpha → Beta"
  lane :promote_alpha_to_beta do
    upload_to_play_store(
      track: 'alpha',
      track_promote_to: 'beta',
      json_key: ENV['JSON_KEY_PATH'],
      package_name: ENV['PACKAGE_NAME'],
      skip_upload_apk: true,
      skip_upload_aab: true,
      skip_upload_metadata: true,
      skip_upload_images: true,
      skip_upload_screenshots: true
    )
    UI.success("🎉 Promoted Alpha → Beta!")
  end

  desc "Promote Beta → Production"
  lane :promote_beta_to_production do
    upload_to_play_store(
      track: 'beta',
      track_promote_to: 'production',
      json_key: ENV['JSON_KEY_PATH'],
      package_name: ENV['PACKAGE_NAME'],
      skip_upload_apk: true,
      skip_upload_aab: true,
      skip_upload_metadata: true,
      skip_upload_images: true,
      skip_upload_screenshots: true
    )
    UI.success("🎉 Promoted Beta → Production!")
  end

  ###########################################################
  # 🧹 Global Hooks
  ###########################################################
  after_all do |lane|
    UI.success("✅ Lane '#{lane}' completed successfully!")
  end

  error do |lane, exception|
    UI.error("❌ Lane '#{lane}' failed with error: #{exception.message}")
  end
end
